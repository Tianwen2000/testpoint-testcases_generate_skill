看具体效果，有时候这个两个需求文档放一起给ai分析会给ai搞混（也许是两个需求文档本身就有误差混淆、写的不规范的原因）

本次为迭代版本需求分析，不是 0-1 项目。
输入材料：
1.上版本需求文档：A
2.本版本需求文档：B
分析目标：
1.以上版本为基线，本版本为目标版本
2.先识别新增 / 修改 / 删除内容
3.再分析本次变更对已有链路的回归影响
4.输出时区分：
- 继承规则
- 新增规则
- 变更规则
- 删除/失效规则
- 回归关注点
5.测试范围以“本次增量 + 受影响回归”为主，不做无意义全量重写
6.请使用需求分析通用skill。
本次为迭代版本需求分析，不是 0-1 项目。
以上版本需求文档为基线版本，本版本需求文档为目标版本。
请按“增量分析 + 受影响回归分析”模式输出，不做无意义全量重写。